
using System;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading;
using NUnit.Framework;
using OpenQA.Selenium;
using OpenQA.Selenium.Chrome;
using OpenQA.Selenium.Support.UI;


namespace WebAddressbookTests
{
    [TestFixture]
    public class AAAAAAAAAA
    {/*
        [Test]
        public void Forr()
        {
            string[] s = new string[] {"I", "wanna", "rock", "!!!!!"};

            for (int i = 0; i < s.Length; i++)
            {
                Console.Out.Write(s[i] + "\n");
            }

        }

        [Test]
        public void Forreach()
        {
            string[] s = new string[] { "I", "wanna", "rock", "!!!!!" };

            foreach (string element in s)
            {
                Console.Out.Write(element + "\n");
            }

        }


        
        [Test]
        public void Whilee()
        {
            IWebDriver driver = null;
            int attempt = 0;

                while (driver.FindElement(By.Id("test")).Count) ==0 && attempt < 60) //счетчик
                {
                System.Threading.Thread.Sleep(1000);   //ждем 1000мс и проверяем снова
                attempt++;
                }

        }
        

        [Test]
        public void Do_while()
        {
            IWebDriver driver = null;
            int attempt = 0;

            do    //тело цикла исполнится сначала в любом случае
                {
                System.Threading.Thread.Sleep(1000);   //ждем 1000мс и проверяем снова
                attempt++;
            }
            while (driver.FindElement(By.Id("test")).Count) == 0 && attempt < 60); //счетчик
        }

*/





    }
}
